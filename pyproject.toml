[project]
name = "agent-worker"
version = "0.1.0"
description = "worker"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "aiodocker>=0.26.0",
    "fastapi>=0.135.1",
    "uvicorn>=0.41.0",
]
